1. var is global scope , hoisted and re-declarable. on the otherhand, let and const are    block scope. var and let is allowing to change their value but const does not. 

2. spread operator is a concept of ES6. it's shows values of array and string also shows object properties.

3. map() is a array method that works in every single element and return a new array.
filter() is also array method that select elements and pass a specific condition and return a new array.
forEach() is loop and does not return.
the similarities of them , they work by looping
and the dissimilarities are map() return a new array by using function, filter() return a new array by using specific condition and forEach is used for getting single element

4. arrow function is function that is written by using =>(arrow) instead the function keyword . it is more readable, short and useful

5. template literals are used to writing backticks(``). it is easier to write multi line and embedded expressions(${expressions}) by using this template


